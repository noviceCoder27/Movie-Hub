{
    "version":2,
    "builds": [
        { "src": "dist/*.js", "use": "@vercel/node" }
    ],
    "routes": [
        {
          "src": "/(.*)",
          "dest": "/dist/$1",
          "methods":["GET","PATCH","POST", "DELETE", "PUT"],
          "headers": {
            "Access-Control-Allow-Origin": "*"
          }
        }
    ]
    
}